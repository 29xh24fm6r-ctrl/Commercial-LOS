// AUTO-GENERATED (Phase 2, regenerated as Phase 3 routes subsystems).
//
// Each entry is a source module deliberately NOT reachable from src/main.tsx yet.
// scripts/reachability-audit.mjs treats these as EXPECTED orphans; anything orphaned
// and NOT listed here is an UNEXPECTED orphan and fails the gate. As a subsystem is
// routed, its now-reachable files drop out automatically on regeneration.
//
// Tags in reasons: WIRE candidate (route next), GATE candidate (keep deal/transport
// scoped), else transitive (reachable once its consumer routes). No deletions performed.

export interface IntentionallyUnroutedModule {
  /** Repo-relative, forward-slashed path (matches reachability-audit output). */
  readonly path: string;
  /** Why it is unrouted today / how it becomes reachable. */
  readonly reason: string;
  /** The phase expected to route (or transitively reach) it. */
  readonly plannedPhase: string;
}

export const INTENTIONALLY_UNROUTED: readonly IntentionallyUnroutedModule[] = [
  // â”€â”€ src/access â”€â”€
  { path: 'src/access/OperatorLaunchConsole.tsx', reason: 'Access/evidence/identity internals; reachable transitively via their consumers.', plannedPhase: 'Phase 3+' },
  { path: 'src/access/adminEntitlementGrantAdapter.ts', reason: 'Access/evidence/identity internals; reachable transitively via their consumers.', plannedPhase: 'Phase 3+' },
  { path: 'src/access/adminEntitlementRevokeAdapter.ts', reason: 'Access/evidence/identity internals; reachable transitively via their consumers.', plannedPhase: 'Phase 3+' },
  { path: 'src/access/finalLaunchSmokeEvidence.ts', reason: 'Access/evidence/identity internals; reachable transitively via their consumers.', plannedPhase: 'Phase 3+' },
  { path: 'src/access/finalLaunchSmokeEvidenceLoader.ts', reason: 'Access/evidence/identity internals; reachable transitively via their consumers.', plannedPhase: 'Phase 3+' },
  { path: 'src/access/operatorLaunchConsoleModel.ts', reason: 'Access/evidence/identity internals; reachable transitively via their consumers.', plannedPhase: 'Phase 3+' },
  { path: 'src/access/operatorSmokeEvidenceRegistry.ts', reason: 'Access/evidence/identity internals; reachable transitively via their consumers.', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/activation â”€â”€
  { path: 'src/activation/adminEntitlementActivation.ts', reason: 'Activation/launch console internals; reachable once the admin activation surface routes (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/activation/borrowerCommsActivation.ts', reason: 'Activation/launch console internals; reachable once the admin activation surface routes (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/activation/checklistGenerationActivation.ts', reason: 'Activation/launch console internals; reachable once the admin activation surface routes (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/activation/crmActivation.ts', reason: 'Activation/launch console internals; reachable once the admin activation surface routes (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/activation/documentUploadActivation.ts', reason: 'Activation/launch console internals; reachable once the admin activation surface routes (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/activation/finalLaunchReadiness.ts', reason: 'Activation/launch console internals; reachable once the admin activation surface routes (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/activation/fullSystemActivation.ts', reason: 'Activation/launch console internals; reachable once the admin activation surface routes (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/activation/launchReadiness.ts', reason: 'Activation/launch console internals; reachable once the admin activation surface routes (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/activation/newDealCreateActivation.ts', reason: 'Activation/launch console internals; reachable once the admin activation surface routes (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/activation/portfolioBoardingActivation.ts', reason: 'Activation/launch console internals; reachable once the admin activation surface routes (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/activation/stageProgressionActivation.ts', reason: 'Activation/launch console internals; reachable once the admin activation surface routes (WIRE candidate).', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/admin â”€â”€
  { path: 'src/admin/AdminEntitlementDiagnosticCard.tsx', reason: 'Admin internals (evidence/readiness models) reachable transitively via the admin surface.', plannedPhase: 'Phase 3+' },
  { path: 'src/admin/checklistSignoffEvidence.ts', reason: 'Admin internals (evidence/readiness models) reachable transitively via the admin surface.', plannedPhase: 'Phase 3+' },
  { path: 'src/admin/controlledLiveCutoverReadiness.ts', reason: 'Admin internals (evidence/readiness models) reachable transitively via the admin surface.', plannedPhase: 'Phase 3+' },
  { path: 'src/admin/fullProductionLaunchEvidence.ts', reason: 'Admin internals (evidence/readiness models) reachable transitively via the admin surface.', plannedPhase: 'Phase 3+' },
  { path: 'src/admin/outlookConnectorEvidence.ts', reason: 'Admin internals (evidence/readiness models) reachable transitively via the admin surface.', plannedPhase: 'Phase 3+' },
  { path: 'src/admin/pacTableAccessEvidence.ts', reason: 'Admin internals (evidence/readiness models) reachable transitively via the admin surface.', plannedPhase: 'Phase 3+' },
  { path: 'src/admin/runtimeVerifiedSchemaBridge.ts', reason: 'Admin internals (evidence/readiness models) reachable transitively via the admin surface.', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/adminConfig â”€â”€
  { path: 'src/adminConfig/AdminConfigurationApplyPreviewPanel.tsx', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/AdminConfigurationPersistenceReadinessPanel.tsx', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/adminConfigurationApplyFeatureFlags.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/adminConfigurationApplyTypes.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/adminConfigurationContentSafety.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/adminConfigurationDataverseSchemaPlan.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/adminConfigurationPersistenceAdapter.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/adminConfigurationPersistenceFeatureFlags.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/adminConfigurationPersistenceMapper.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/adminConfigurationPersistenceTypes.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/adminConfigurationTransport.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/buildAdminConfigurationApplyPlan.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/buildAdminConfigurationProposal.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/createAdminConfigurationControlledApplyEngine.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/createAdminConfigurationDataversePersistenceAdapter.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/createDisabledAdminConfigurationPersistenceAdapter.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/deriveAdminConfigurationApplyReadiness.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/deriveAdminConfigurationApplyWorkflow.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/deriveAdminConfigurationReviewDecision.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/deriveAdminConfigurationSchemaReadiness.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/resolveAdminConfigurationPersistenceAdapter.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/adminConfig/validateAdminConfigurationProposal.ts', reason: 'Admin config review routed at /surfaces/admin-config; remaining apply/persistence panels are operator-data-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/annualReview â”€â”€
  { path: 'src/annualReview/AnnualReviewBorrowerRequestPanel.tsx', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/AnnualReviewFinancialCovenantPanel.tsx', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/AnnualReviewPackagePreviewPanel.tsx', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/annualReviewBorrowerRequestTypes.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/annualReviewDeliveryFeatureFlags.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/annualReviewDeliveryTypes.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/annualReviewEmailDeliveryAdapter.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/annualReviewFinancialFacts.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/annualReviewFinancialTypes.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/annualReviewPackageExportAdapter.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/annualReviewPackageTypes.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/annualReviewRequestFeatureFlags.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/annualReviewSmsDeliveryAdapter.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/annualReviewUploadLinkAdapter.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/buildAnnualReviewBoardPackage.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/buildAnnualReviewBorrowerRequestDraft.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/buildAnnualReviewBorrowerRequestPackage.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/buildAnnualReviewDeliveryAuditSummary.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/buildAnnualReviewEvidenceIndex.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/buildAnnualReviewFdicPackage.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/buildAnnualReviewFinancialMemoSections.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/buildAnnualReviewMemoPackage.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/deriveAnnualReviewBorrowerRequestWorkflow.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/deriveAnnualReviewFinancialAnalysisSnapshot.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/deriveAnnualReviewFinancialReadiness.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/deriveAnnualReviewFinancialSpreadSnapshot.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/deriveAnnualReviewPackageReadiness.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/deriveAnnualReviewPackageWorkflow.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/financialTestFixtures.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/packageTestFixtures.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/resolveAnnualReviewBorrowerRequestRecipients.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/resolveAnnualReviewCovenantDefinitions.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/resolveAnnualReviewDeliveryAdapters.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/testAnnualReviewCovenants.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/annualReview/validateAnnualReviewDeliveryRequest.ts', reason: 'Annual review panels require a live loan/cycle/master data context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/banker â”€â”€
  { path: 'src/banker/BankerCrmIntelligencePanel.tsx', reason: 'Banker CRM intelligence panel reachable via the banker workspace once expanded.', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/committee â”€â”€
  { path: 'src/committee/CreditPackageExportPanel.tsx', reason: 'Credit committee routed at /surfaces/committee; remaining export/e-sign panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/committee/ESignEnvelopePanel.tsx', reason: 'Credit committee routed at /surfaces/committee; remaining export/e-sign panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/committee/creditPackageExportAdapter.ts', reason: 'Credit committee routed at /surfaces/committee; remaining export/e-sign panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/committee/eSignEnvelopeAdapter.ts', reason: 'Credit committee routed at /surfaces/committee; remaining export/e-sign panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/copilot â”€â”€
  { path: 'src/copilot/copilotAuditLogger.ts', reason: 'Copilot transport/adapters are deal-scoped and transport-gated; surfaced inside the deal workspace, not standalone (GATE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/copilot/copilotConnectorConfig.ts', reason: 'Copilot transport/adapters are deal-scoped and transport-gated; surfaced inside the deal workspace, not standalone (GATE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/copilot/copilotCustomApiAdapter.ts', reason: 'Copilot transport/adapters are deal-scoped and transport-gated; surfaced inside the deal workspace, not standalone (GATE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/copilot/copilotCustomApiContract.ts', reason: 'Copilot transport/adapters are deal-scoped and transport-gated; surfaced inside the deal workspace, not standalone (GATE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/copilot/copilotDataverseCustomApiTransport.ts', reason: 'Copilot transport/adapters are deal-scoped and transport-gated; surfaced inside the deal workspace, not standalone (GATE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/copilot/copilotLiveTransportHarness.ts', reason: 'Copilot transport/adapters are deal-scoped and transport-gated; surfaced inside the deal workspace, not standalone (GATE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/copilot/copilotServerDeploymentReadiness.ts', reason: 'Copilot transport/adapters are deal-scoped and transport-gated; surfaced inside the deal workspace, not standalone (GATE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/copilot/copilotServerHandler.ts', reason: 'Copilot transport/adapters are deal-scoped and transport-gated; surfaced inside the deal workspace, not standalone (GATE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/copilot/copilotTransportReadiness.ts', reason: 'Copilot transport/adapters are deal-scoped and transport-gated; surfaced inside the deal workspace, not standalone (GATE candidate).', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/crm â”€â”€
  { path: 'src/crm/write/crmUpdateAdapter.ts', reason: 'Governed CRM field-update adapter (Phase 6): wired + certified (default-off, fail-closed) but its in-drawer InlineEdit UI is the deferred integration; consumed via makeOrgFieldSaver once CRM persistence is armed (WIRE candidate).', plannedPhase: 'Phase 6+' },
  { path: 'src/crm/CrmAccountSurfaces.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/CrmActivityTimeline.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/CrmAdminControlPanel.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/CrmContactTaskBoard.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/CrmRelationshipCommandCenter.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/CrmRelationshipHealthCard.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/CrmRelationshipNetworkPanel.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/CrmRollupCards.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/CrmSpineReadinessConsole.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/CrmSpineRecoveryConsole.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/activityTimeline/CrmActivityTimelinePanel.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/activityTimeline/crmActivityTimelineModel.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  // CRM-C: CrmCommandCenter.tsx + crmCommandCenterViewModel.ts are now reachable via the
  // routed CrmCommandCenterRoute (/surfaces/crm-command-center) and dropped from this list.
  { path: 'src/crm/commandCenter/CrmCommandCenterShell.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/commandCenter/CrmRelationshipIntelligenceStory.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/commandCenter/CrmWorkspaceEntryCard.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmAccountViewModel.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmActivityTaskModel.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmAdminControlModel.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmDataverseMapper.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmDataverseSchemaPlan.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmFullSchemaContract.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmLiveDataverseAdapter.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmLiveDataverseTransport.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmManagerTeamMountReadiness.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmPersistenceAdapter.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmPersistenceTypes.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmRelationshipHealthModel.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmRelationshipIdempotency.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmRelationshipRollups.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmRuntimeSchemaGate.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmSalesforceSpineApplyOrchestrator.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmSalesforceSpineAudit.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmSalesforceSpineLaunchReadiness.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmSalesforceSpineLiveGates.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmSalesforceSpineModel.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmSalesforceSpineNewDealLinkage.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmSalesforceSpinePersistenceAdapter.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmSalesforceSpineSchemaAdapter.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmSdkContract.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/crmWritebackAdapter.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/dailyActions/BankerCrmDailyActionQueue.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/dailyActions/bankerCrmDailyActionQueue.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/deriveCrmSchemaInspectionReport.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/deriveCrmSchemaSeedPlan.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/executiveStrategy/ExecutiveCrmStrategyView.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/executiveStrategy/executiveCrmStrategyViewModel.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/managerIntelligence/ManagerCrmPipelineIntelligence.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/managerIntelligence/managerCrmPipelineIntelligence.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/matching/crmEntityMatchingModel.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/ncinoLane/NcinoLane.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/ncinoLane/ncinoLaneViewModel.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/relationshipIntelligence/CrmRelationshipIntelligenceCockpit.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/relationshipIntelligence/crmRelationshipDrillThrough.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/relationshipIntelligence/crmRelationshipIntelligenceDrillthrough.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/relationshipIntelligence/crmRelationshipIntelligenceViewModel.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/resolveCrmPersistenceAdapter.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/salesforceLane/SalesforceLane.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/salesforceLane/salesforceLaneViewModel.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/sourceOfTruth/CrmSourceOfTruthCockpit.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/sourceOfTruth/crmSourceOfTruthCockpitViewModel.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/sourceOfTruth/crmSourceOfTruthMap.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/syncPreview/CrmSyncPreviewCockpit.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/syncPreview/crmSyncPreviewCockpitViewModel.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/syncPreview/crmSyncPreviewPlan.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/writeback/CrmDryRunWritebackCommandCenter.tsx', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/writeback/crmAllowlistedLiveWritePilot.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/writeback/crmControlledWritebackAdapter.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/writeback/crmDryRunWritebackCommandViewModel.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/crm/writeback/crmWritebackPolicyGate.ts', reason: 'CRM relationship command center â€” built read surfaces not yet routed; CrmHubWorkspace is already mounted in the banker workspace. Standalone read-only routing deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/deals â”€â”€
  { path: 'src/deals/documentChecklistUiEnableReadiness.ts', reason: 'Deal sub-feature module (checklist UI generation) not yet wired into DealRoute (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/deals/documentChecklistUiGenerationAction.ts', reason: 'Deal sub-feature module (checklist UI generation) not yet wired into DealRoute (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/deals/newDealChecklistGenerationLiveDeps.ts', reason: 'Deal sub-feature module (checklist UI generation) not yet wired into DealRoute (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/deals/documentChecklistFileFields.ts', reason: 'Dataverse remediation — STOPGAP type augmentation for the six new cr664_documentchecklist upload columns (scripts/dataverse/create-document-checklist-file-columns.ps1). Nothing reads these fields yet since they do not exist live; delete once a real SDK regen makes them part of Cr664_documentchecklistsModel.ts (WIRE candidate).', plannedPhase: 'Operator enablement' },
  // â”€â”€ src/executive â”€â”€
  { path: 'src/executive/ProductProfitabilityAvailabilityPanel.tsx', reason: 'Executive sub-surface not yet routed (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/executive/productProfitabilityAvailabilityModel.ts', reason: 'Executive sub-surface not yet routed (WIRE candidate).', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/generated â”€â”€
  { path: 'src/generated/index.ts', reason: 'Generated Dataverse service/model; reachable transitively once a consuming subsystem routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/generated/models/Cr664_borrowersModel.ts', reason: 'Generated Dataverse service/model; reachable transitively once a consuming subsystem routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/generated/models/Cr664_clientrelationshipsModel.ts', reason: 'Generated Dataverse service/model; reachable transitively once a consuming subsystem routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/generated/models/Cr664_naicsindustrymapsModel.ts', reason: 'Generated Dataverse service/model for the cr664_naicsindustrymap table; the CRM industry projection uses the local naicsSectorMap.ts derivation instead, so this generated pair stays unconsumed. Reachable transitively if a subsystem later reads the live table directly.', plannedPhase: 'Phase 3+' },
  { path: 'src/generated/models/Cr664_portfolioboardedloanevidencesModel.ts', reason: 'Generated Dataverse service/model; reachable transitively once a consuming subsystem routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/generated/models/Cr664_teamsModel.ts', reason: 'Generated Dataverse service/model; reachable transitively once a consuming subsystem routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/generated/models/Cr664_usersModel.ts', reason: 'Generated Dataverse service/model; reachable transitively once a consuming subsystem routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/generated/services/Cr664_borrowersService.ts', reason: 'Generated Dataverse service/model; reachable transitively once a consuming subsystem routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/generated/services/Cr664_clientrelationshipsService.ts', reason: 'Generated Dataverse service/model; reachable transitively once a consuming subsystem routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/generated/services/Cr664_naicsindustrymapsService.ts', reason: 'Generated Dataverse service/model for the cr664_naicsindustrymap table; the CRM industry projection uses the local naicsSectorMap.ts derivation instead, so this generated pair stays unconsumed. Reachable transitively if a subsystem later reads the live table directly.', plannedPhase: 'Phase 3+' },
  { path: 'src/generated/services/Cr664_portfolioboardedloanevidencesService.ts', reason: 'Generated Dataverse service/model; reachable transitively once a consuming subsystem routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/generated/services/Cr664_teamsService.ts', reason: 'Generated Dataverse service/model; reachable transitively once a consuming subsystem routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/generated/services/Cr664_usersService.ts', reason: 'Generated Dataverse service/model; reachable transitively once a consuming subsystem routes.', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/integrations â”€â”€
  { path: 'src/integrations/IntegrationRequestPreviewPanel.tsx', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/coreBanking/CoreBankingLookupPanel.tsx', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/coreBanking/coreBankingLookupAdapter.ts', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/createDisabledIntegrationAdapters.ts', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/externalPlatforms/ExternalAllowlistedWritePilotPanel.tsx', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/externalPlatforms/ExternalDryRunWritebackPanel.tsx', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/externalPlatforms/ExternalEntityMatchReviewPanel.tsx', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/externalPlatforms/ExternalPlatformConnectorReadinessPanel.tsx', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/externalPlatforms/ExternalPlatformReadOnlyPanel.tsx', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/externalPlatforms/LiveExternalSyncPreviewPanel.tsx', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/externalPlatforms/externalAllowlistedWritePilot.ts', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/externalPlatforms/externalDryRunWritebackPlan.ts', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/externalPlatforms/externalEntityMatchAgainstLiveRecords.ts', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/externalPlatforms/externalPlatformConnectorReadiness.ts', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/externalPlatforms/externalPlatformReadOnlyAdapter.ts', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/externalPlatforms/externalPlatformRecordMapper.ts', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/externalPlatforms/externalWritebackSchemaValidator.ts', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/externalPlatforms/liveExternalSyncPreview.ts', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/integrationAdapterContracts.ts', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/policyGates/AmlKycCreditBureauPolicyGatePanel.tsx', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/policyGates/amlKycCreditBureauPolicyGate.ts', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/integrations/resolveIntegrationAdapter.ts', reason: 'Integration registry routed at /surfaces/integrations; remaining external-platform/policy-gate panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/navigation â”€â”€
  { path: 'src/navigation/intentionallyUnrouted.ts', reason: 'This allow-list itself: consumed by the reachability gate + its test, not by the app import graph. Intentionally never reachable from main.tsx.', plannedPhase: 'never' },
  { path: 'src/navigation/workspaceScreens.ts', reason: 'Navigation metadata module reachable once consumed by the routed nav.', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/platform â”€â”€
  { path: 'src/platform/ProductProcessTemplateCatalogPanel.tsx', reason: 'Platform metadata catalog routed at /surfaces/platform-catalog; remaining object/view/template sub-panels reachable once the dashboard expands.', plannedPhase: 'Phase 3+' },
  { path: 'src/platform/ProductProcessTemplateSelectionPanel.tsx', reason: 'Platform metadata catalog routed at /surfaces/platform-catalog; remaining object/view/template sub-panels reachable once the dashboard expands.', plannedPhase: 'Phase 3+' },
  { path: 'src/platform/derivePlatformObjectModel.ts', reason: 'Platform metadata catalog routed at /surfaces/platform-catalog; remaining object/view/template sub-panels reachable once the dashboard expands.', plannedPhase: 'Phase 3+' },
  { path: 'src/platform/derivePlatformViews.ts', reason: 'Platform metadata catalog routed at /surfaces/platform-catalog; remaining object/view/template sub-panels reachable once the dashboard expands.', plannedPhase: 'Phase 3+' },
  { path: 'src/platform/deriveProductProcessProfile.ts', reason: 'Platform metadata catalog routed at /surfaces/platform-catalog; remaining object/view/template sub-panels reachable once the dashboard expands.', plannedPhase: 'Phase 3+' },
  { path: 'src/platform/deriveProductProcessRequirements.ts', reason: 'Platform metadata catalog routed at /surfaces/platform-catalog; remaining object/view/template sub-panels reachable once the dashboard expands.', plannedPhase: 'Phase 3+' },
  { path: 'src/platform/deriveProductProcessTemplateReadiness.ts', reason: 'Platform metadata catalog routed at /surfaces/platform-catalog; remaining object/view/template sub-panels reachable once the dashboard expands.', plannedPhase: 'Phase 3+' },
  { path: 'src/platform/deriveProductProcessTemplateSelection.ts', reason: 'Platform metadata catalog routed at /surfaces/platform-catalog; remaining object/view/template sub-panels reachable once the dashboard expands.', plannedPhase: 'Phase 3+' },
  { path: 'src/platform/productProcessTemplateRegistry.ts', reason: 'Platform metadata catalog routed at /surfaces/platform-catalog; remaining object/view/template sub-panels reachable once the dashboard expands.', plannedPhase: 'Phase 3+' },
  { path: 'src/platform/productProcessTemplateTypes.ts', reason: 'Platform metadata catalog routed at /surfaces/platform-catalog; remaining object/view/template sub-panels reachable once the dashboard expands.', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/portfolioAnnualReview â”€â”€
  { path: 'src/portfolioAnnualReview/AnnualReviewTaskBoard.tsx', reason: 'Annual portfolio review routed at /surfaces/portfolio-annual-review; remaining row/queue sub-panels reachable once the command center expands.', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioAnnualReview/BorrowerFinancialRequestPreview.tsx', reason: 'Annual portfolio review routed at /surfaces/portfolio-annual-review; remaining row/queue sub-panels reachable once the command center expands.', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioAnnualReview/annualReviewPersistenceAdapter.ts', reason: 'Annual portfolio review routed at /surfaces/portfolio-annual-review; remaining row/queue sub-panels reachable once the command center expands.', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioAnnualReview/annualReviewPersistenceTypes.ts', reason: 'Annual portfolio review routed at /surfaces/portfolio-annual-review; remaining row/queue sub-panels reachable once the command center expands.', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/portfolioBoarding â”€â”€
  { path: 'src/portfolioBoarding/BoardLoanReviewPackagePreview.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/FdicBoardPackageWorkspace.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/FdicEvidenceIndex.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/FdicExaminerPackagePreview.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/FdicPackageSectionList.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioBoardingPackageExportModel.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingAuditPanel.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingCreateFlow.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingPreview.tsx', reason: 'Superseded at /surfaces/portfolio-boarding by the editable PortfolioLoanBoardingForm.tsx; kept as a reusable read-only view for a future "view boarded loan" surface (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingReadOnlySections.tsx', reason: 'Exclusive to the now-unrouted PortfolioLoanBoardingPreview.tsx (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingReadinessPanel.tsx', reason: 'Exclusive to the now-unrouted PortfolioLoanBoardingPreview.tsx (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingDocumentInventory.tsx', reason: 'Exclusive to the now-unrouted PortfolioLoanBoardingPreview.tsx (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingEvidencePanel.tsx', reason: 'Exclusive to the now-unrouted PortfolioLoanBoardingPreview.tsx (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingDetail.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingDocumentManager.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingDocumentUploadPanel.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingEditor.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingEvidenceManager.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingExaminerNotes.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingList.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingSaveBar.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingSearchPanel.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingStatusBanner.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingValidationSummary.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioLoanBoardingWorkspace.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/PortfolioManagerReviewPackagePreview.tsx', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/derivePortfolioBoardingSchemaInspectionReport.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/derivePortfolioBoardingSchemaSeedPlan.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/derivePortfolioBoardingSchemaVerificationReport.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/loadPortfolioBoardedLoansForWorkspace.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioBoardedLoanCommandCenterRows.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioBoardingAccess.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioBoardingCommandCenterAdapter.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioBoardingFeatureFlags.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioBoardingListRows.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioFullSchemaArtifact.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioLoanBoardingAuditTrail.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioLoanBoardingDataverseAdapter.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioLoanBoardingDataverseSchemaPlan.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioLoanBoardingWriteAdapter.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioLoanDocumentUploadAdapter.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioRelationshipIdempotency.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioSharePointDocumentAdapters.ts', reason: 'Phase 264 (P0) SharePoint document upload â€” consumed by the document upload panel, itself data-context-scoped and unrouted alongside the rest of portfolio loan boarding (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioSharePointDocumentMode.ts', reason: 'Phase 264 (P0) SharePoint document upload â€” consumed by the document upload panel, itself data-context-scoped and unrouted alongside the rest of portfolio loan boarding (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioSharePointDocumentPort.ts', reason: 'Phase 264 (P0) SharePoint document upload â€” consumed by the document upload panel, itself data-context-scoped and unrouted alongside the rest of portfolio loan boarding (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/portfolioSharePointDocumentSchemaPlan.ts', reason: 'Phase 264 (P0) SharePoint document upload â€” consumed by the document upload panel, itself data-context-scoped and unrouted alongside the rest of portfolio loan boarding (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/resolvePortfolioLoanBoardingAdapter.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolioBoarding/usePortfolioLoanDocumentPersistence.ts', reason: 'Portfolio loan boarding â€” boarding preview routed at /surfaces/portfolio-boarding; remaining editor/document/FDIC panels are data-context-scoped (WIRE candidate).', plannedPhase: 'Phase 3+' },
  // -- src/portfolio (PE-2/4/5/9 analytics: supporting modules of already-routed panels) --
  { path: 'src/portfolio/reconciliation/reconciliationControlSchemaPlan.ts', reason: 'PE-2 migration-control Dataverse schema PLAN (constants only, no IO/route); consumed by schema inspect/verify tooling, mirroring the boarded-loan schema plan. Provisioning artifact, never app-routed by design. The BookReconciliationPanel it plans for IS routed in the Portfolio Command Center.', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolio/profitability/profitabilityLinkSchemaPlan.ts', reason: 'PE-4 additive profitability Dataverse schema PLAN (constants only, no IO/route); schema-provisioning artifact consumed by inspect/verify tooling. The PortfolioProfitabilityPanel it supports IS routed in the Portfolio Command Center.', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolio/riskRating/riskRatingSchemaPlan.ts', reason: 'PE-5 dual risk-rating/classification Dataverse schema PLAN (constants only, no IO/route); schema-provisioning artifact. The PortfolioClassificationPanel it supports IS routed in the Portfolio Command Center.', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolio/profitability/LoanProfitabilityCard.tsx', reason: 'PE-4 per-loan read-only profitability drill-down card; its portfolio-level rollup (PortfolioProfitabilityPanel) IS routed. Per-loan drilldown surfacing needs a per-loan data flow not yet built on this route (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolio/riskRating/RiskRatingCard.tsx', reason: 'PE-5 per-loan read-only dual-rating drill-down card; its portfolio-level rollup (PortfolioClassificationPanel) IS routed. Per-loan drilldown surfacing needs a per-loan data flow not yet built on this route (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/portfolio/covenants/covenantMonitoring.ts', reason: 'PE-9 pure covenant-testing engine (DSCR/leverage/liquidity/TNW/current-ratio from spread financials); the CovenantReviewPanel rollup IS routed. The per-loan covenant data flow to feed it is not built on this route yet (WIRE candidate).', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/servicing â”€â”€
  { path: 'src/servicing/ServicingLifecycleMapperPanel.tsx', reason: 'Servicing lifecycle panel requires a live servicing snapshot/stage context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/servicing/ServicingLifecyclePanel.tsx', reason: 'Servicing lifecycle panel requires a live servicing snapshot/stage context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/servicing/deriveServicingCollateralSecurityStatus.ts', reason: 'Servicing lifecycle panel requires a live servicing snapshot/stage context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/servicing/deriveServicingCovenantReportingStatus.ts', reason: 'Servicing lifecycle panel requires a live servicing snapshot/stage context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/servicing/deriveServicingInsuranceTicklerStatus.ts', reason: 'Servicing lifecycle panel requires a live servicing snapshot/stage context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/servicing/deriveServicingLifecycleSnapshot.ts', reason: 'Servicing lifecycle panel requires a live servicing snapshot/stage context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/servicing/deriveServicingLifecycleStage.ts', reason: 'Servicing lifecycle panel requires a live servicing snapshot/stage context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/servicing/deriveServicingMaturityRenewalStatus.ts', reason: 'Servicing lifecycle panel requires a live servicing snapshot/stage context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/servicing/deriveServicingObligations.ts', reason: 'Servicing lifecycle panel requires a live servicing snapshot/stage context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/servicing/servicingLifecycleMapper.ts', reason: 'Servicing lifecycle panel requires a live servicing snapshot/stage context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/servicing/servicingLifecycleTypes.ts', reason: 'Servicing lifecycle panel requires a live servicing snapshot/stage context; standalone read-only preview deferred (WIRE candidate).', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/shared â”€â”€
  { path: 'src/shared/annualReview/annualReviewTaskEngine.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/annualReview/deriveBorrowerFinancialRequestPackage.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/crm/crmDataverseSchemaPlan.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/crm/crmIntegrationSeams.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/crm/crmTypes.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/governance/productionSurfaceInventory.ts', reason: 'Factory Arc Phase 1 data-only inventory, consumed today only by bankerFacingLaunchLanguageGuard.test.ts (a governance test, not a runtime import). Becomes reachable transitively once Phase 4 folds it into the Platform Operations workspace.', plannedPhase: 'Phase 4' },
  { path: 'src/shared/crm/deriveCrmContactTasks.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/crm/deriveCrmReadiness.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/crm/deriveCrmRelationshipNetworkSnapshot.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/crm/resolveBorrowerRequestRecipient.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/drillthrough/drillThroughRegistry.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/fdic/fdicEvidenceArchitecture.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/fdic/fdicRemediationArchitectureSnapshot.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/fdic/fdicRemediationOperatingModel.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/fdic/fdicRemediationRoadmap.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/fdic/fdicWorkspaceResponsibilityMap.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/portfolioBoarding/fdicExaminerPackage.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/portfolioBoarding/portfolioLoanBoardingSnapshot.ts', reason: 'Exclusive to the now-unrouted PortfolioLoanBoardingPreview.tsx (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/portfolioBoarding/portfolioLoanEvidenceBinder.ts', reason: 'Exclusive to the now-unrouted PortfolioLoanBoardingEvidencePanel.tsx (WIRE candidate).', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/portfolioBoarding/portfolioLoanDocumentClassifier.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  { path: 'src/shared/portfolioBoarding/portfolioLoanDocumentReadiness.ts', reason: 'Shared helper/model consumed by an unrouted subsystem; becomes reachable transitively once its consumer routes.', plannedPhase: 'Phase 3+' },
  // â”€â”€ src/workflow â”€â”€
  { path: 'src/workflow/deriveWorkflowRoute.ts', reason: 'A separate, older (Phase 142A) route-derivation engine with its own type vocabulary (workflowRouteRegistry.ts/workflowRoutingTypes.ts); superseded by the live Phase 142C engine (deriveConfigurableWorkflowRoute.ts) and never mounted.', plannedPhase: 'Phase 3+' },
  // Stage Advancement engine + control (RETURN/DECLINE/WITHDRAW only -- forward ADVANCE is LIVE via
  // the separate DealStageProgressionCard.tsx surface): built and tested, but this specific
  // control/engine pair is WIRED_DISABLED because it is not mounted in any live workspace.
  // AUTO_STAGE_ADVANCE_ENABLED is itself ARMED (true, since WF-1A) -- it is NOT the remaining
  // blocker here; mounting is. See docs/STAGE_PROGRESSION_ENABLEMENT_MAP.md.
  { path: 'src/workflow/StageWorkflowControl.tsx', reason: 'Stage Advancement control for RETURN/DECLINE/WITHDRAW; mounted in the deal workspace once hosted (GATE candidate, WIRED_DISABLED -- unmounted, not flag-gated: AUTO_STAGE_ADVANCE_ENABLED is already armed).', plannedPhase: 'Operator enablement' },
  { path: 'src/workflow/canonicalStageTransition.ts', reason: 'Stage Advancement governed transition engine for RETURN/DECLINE/WITHDRAW; unhosted (GATE candidate, WIRED_DISABLED -- unmounted, not flag-gated: AUTO_STAGE_ADVANCE_ENABLED is already armed; forward ADVANCE is live via the separate DealStageProgressionCard.tsx path).', plannedPhase: 'Operator enablement' },
  { path: 'src/workflow/stageGateContract.ts', reason: 'Stage Advancement exit-gate contract; reachable once StageWorkflowControl is hosted in the deal workspace (transitive, WIRED_DISABLED).', plannedPhase: 'Operator enablement' },
  { path: 'src/workflow/approvalAuthorityMatrix.ts', reason: 'Stage Advancement OGB single authorized-approver policy; reachable once the CREDIT_APPROVAL gate is wired into a hosted control (transitive, WIRED_DISABLED).', plannedPhase: 'Operator enablement' },
  { path: 'src/workflow/losWorkflowTruthMatrix.ts', reason: 'PR 0 machine-readable workflow truth matrix — inert descriptive data for the LOS Full Workflow Activation ARC (docs/LOS_WORKFLOW_TRUTH_MATRIX.md); not wired into any gate, write path, or UI.', plannedPhase: 'ARC documentation' },
  // ── LOS Full Workflow Activation ARC — WIP reconciliation (metadata only) ──
  // These 12 modules are in-flight toward the LOS Full Workflow Activation ARC
  // (docs/LOS_FULL_WORKFLOW_ACTIVATION_ARC.md). They are inert and unwired today — imported
  // only by each other + their own tests, never from a routed surface. Registered here so the
  // reachability audit distinguishes them from unexpected orphans. Each is ADOPTED (wired) by
  // its target ARC PR, which removes its entry below when the domain goes live. No runtime
  // behavior, gate, feature flag, write path, certification, or evidence is changed by this entry.
  { path: 'src/access/stageAdvancementSmokeProof.ts', reason: 'ARC WIP: machine-proven stage-transition smoke-proof infrastructure (attribution, affected record ids, readback). Inert/pure; not wired into any smoke harness yet.', plannedPhase: 'ARC PR 23/24' },
  { path: 'src/crm/certification/crmTeamReadinessCertification.ts', reason: 'ARC WIP: CRM team-readiness certification infrastructure (composes existing readiness dimensions into one verdict). Inert/pure; not mounted.', plannedPhase: 'ARC PR 22' },
  { path: 'src/crm/readiness/crmRoleMountRegistry.ts', reason: 'ARC WIP: CRM role-mount readiness registry (frozen config of which roles have CRM mounted). Inert data; not consumed by a live surface yet.', plannedPhase: 'ARC PR 22' },
  { path: 'src/crm/seed/crmCanonicalSeedReadiness.ts', reason: 'ARC WIP: CRM canonical seed/backfill readiness model over measured facts. Inert/pure; not mounted.', plannedPhase: 'ARC PR 22' },
  { path: 'src/deals/buildLiveCanonicalTransitionDeps.ts', reason: 'ARC WIP: live Dataverse deps for the canonical Return/Decline/Withdraw transition engine (forward Advance is live via the separate DealStageProgressionCard.tsx surface). SAFETY: contains live write sinks (deal update + audit/timeline create) but is INERT while unrouted + unmounted -- AUTO_STAGE_ADVANCE_ENABLED is already armed (true); unmounting, not the flag, is what keeps this inert.', plannedPhase: 'ARC PR 10/11/12/21' },
  { path: 'src/deals/dealReferenceResolvers.ts', reason: 'ARC WIP: fail-closed stage/status reference-code resolvers. SAFETY: live-READ only via guarded dynamic import; inert while unrouted (imported only by buildLiveCanonicalTransitionDeps).', plannedPhase: 'ARC PR 10/11/12/21' },
  { path: 'src/deals/loadStageSeedReadiness.ts', reason: 'ARC WIP: live loader for stage-seed readiness proof. SAFETY: live-READ only via guarded dynamic import; inert while unrouted.', plannedPhase: 'ARC PR 4 or PR 23' },
  { path: 'src/workflow/stageExitGateReconciliation.ts', reason: 'ARC WIP: pure reconciliation between the shallow live transition gate and the rigorous exit gate; blocks certification on untracked facts. Inert/pure; not the live gate.', plannedPhase: 'ARC PR 22' },
  { path: 'src/workflow/stageSeedReadiness.ts', reason: 'ARC WIP: pure deterministic stage-seed readiness evaluator (7 canonical stages, active, unique ascending sequence). Inert/pure; not gate-consumed yet.', plannedPhase: 'ARC PR 4 or PR 23' },
  // Stage reconciliation: these governed workflow-action surfaces were hosted by the
  // retired legacy Loan Workflow Command Center (11-stage spine). They are re-hosted by
  // the canonical StageWorkflowControl when the stage-progression domain is seeded + armed.
  { path: 'src/workflow/LoanWorkflowCommandCenter.tsx', reason: 'Retired legacy 11-stage Opportunity/Qualification command center; unmounted from the deal cockpit by stage reconciliation (the canonical Stage Map supersedes it). Kept for its non-cockpit consumers; DELETE candidate once those repoint.', plannedPhase: 'Follow-up' },
  { path: 'src/workflow/AdvanceWorkflowStageButton.tsx', reason: 'Governed Advance-Stage action surface; previously hosted by the retired command center, re-hosted by StageWorkflowControl when the stage domain is seeded + armed (WIRED_DISABLED).', plannedPhase: 'Operator enablement' },
  { path: 'src/workflow/GenerateWorkflowTasksButton.tsx', reason: 'Governed task-generation action; previously hosted by the retired command center, re-hosted once its domain is armed (WIRED_DISABLED).', plannedPhase: 'Operator enablement' },
  // ── LOS Full Workflow Activation ARC — Phase 1 requirement registry + engine (inert foundation) ──
  // The canonical requirement registry + evaluation engine + types. INERT this phase: not wired into
  // any UI, gate, or write path (imported only by their own test). They flip no gate and change no
  // runtime behavior; the compatibility adapter delegates tracked-fact evaluation to the existing live
  // readiness so Intake → Underwriting stays identical. A later ARC PR (PR 3/20) wires them into the
  // Stage Map / write policy and removes these entries. See docs/LOS_FULL_WORKFLOW_ACTIVATION_ARC.md.
  { path: 'src/workflow/loanWorkflowRequirementTypes.ts', reason: 'ARC Phase 1: pure requirement types (categories/roles/resolver surfaces/backing/status/result shapes). Inert; consumed by the registry + engine only.', plannedPhase: 'ARC Phase 1 (wired PR 3/20)' },
  { path: 'src/workflow/loanWorkflowRequirementRegistry.ts', reason: 'ARC Phase 1: canonical requirement registry (shallow derived from stage defs + deep untracked facts + non-forward placeholders). Inert data; not gate-consumed yet.', plannedPhase: 'ARC Phase 1 (wired PR 3/20)' },
  { path: 'src/workflow/loanWorkflowRequirementEngine.ts', reason: 'ARC Phase 1: requirement evaluation engine (met/unmet/untracked/unavailable, blocking vs recommended, where-to-resolve). Compatibility adapter delegates tracked facts to the existing live readiness. Inert; wired by a later ARC PR.', plannedPhase: 'ARC Phase 1 (wired PR 3/20)' },
  // â”€â”€ src/workspaces â”€â”€
  { path: 'src/workspaces/WorkspaceShell.tsx', reason: 'Workspace shell sub-surface not yet routed.', plannedPhase: 'Phase 3+' },
];

/** Set of allow-listed paths for O(1) membership checks. */
export const INTENTIONALLY_UNROUTED_PATHS: ReadonlySet<string> = new Set(
  INTENTIONALLY_UNROUTED.map((m) => m.path),
);


