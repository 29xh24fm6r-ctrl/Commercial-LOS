import { WorkspaceShell } from './WorkspaceShell';

export function AdminWorkspace() {
  return (
    <WorkspaceShell
      title="Admin Workspace"
      subtitle="Users, roles, permissions, governance diagnostics, audit."
    />
  );
}
