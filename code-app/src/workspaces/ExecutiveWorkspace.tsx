import { WorkspaceShell } from './WorkspaceShell';

export function ExecutiveWorkspace() {
  return (
    <WorkspaceShell
      title="Executive / Board Workspace"
      subtitle="Read-only, snapshot-only, governance-labeled KPIs and trends."
    />
  );
}
