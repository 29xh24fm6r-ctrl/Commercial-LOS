import { WorkspaceShell } from './WorkspaceShell';

export function TeamWorkspace() {
  return (
    <WorkspaceShell
      title="Team Workspace"
      subtitle="Shared lending pipeline, support queues, and team-level alerts."
    />
  );
}
