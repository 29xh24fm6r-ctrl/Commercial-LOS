# Final Production Completion — D1–D20 Disposition Table

**Branch:** `fix/final-production-completion`, based on synced `master` @
`1099d43f08948b25f2f9958c157a755afe2f022e` (PR #97 merge point). Every finding below was reproduced
(or disproved) against CURRENT code, not assumed from the historical audit. Citations are exact
file/line references and test names.

| ID | Finding | Disposition | Evidence / citations | Action this pass |
|---|---|---|---|---|
| D1 | NAICS-to-deal-industry mapping missing/blocking | **PARTIALLY FIXED** | One authoritative pipeline: `src/crm/naics/naicsSectorMap.ts` → `src/crm/naics/naicsIndustryMap.ts` → `src/crm/dealIndustryProjection.ts` → `src/deals/dealIndustryHydration.ts` → `src/deals/hydrateDealIndustryFromCrm.ts`. Auto-hydrates on CRM link (Workstream D), never overwrites a manual value, honestly reports `no-mapping` rather than fabricating (never blocks on an unmapped sector). Gate clears via governed `updateDealProfile` write, not cosmetically. **Residual, non-code gap:** only 5 of 20 NAICS sectors are seeded (`scripts/seed-naics-industry-map.mjs`); 722511 and 561422 both fall in intentionally-unmapped sectors and correctly resolve `no-mapping`, never a wrong guess. Widening sector coverage is a data/business decision, not a defect. | None — already correct; no code change |
| D2 | LOS-originated deal could not complete full lifecycle | **PARTIALLY FIXED / OPERATOR-DEPENDENT for the security floor** | Full lifecycle truth matrix (see companion architecture map). Every transition INTAKE→BOARDED writes for real through `stageAdvanceWriteDependency.ts`/`buildLiveStageAdvanceDeps.ts` with readback-verified persistence, audit, and timeline. `AUTO_STAGE_ADVANCE_ENABLED=true`. Return/Decline/Withdraw are live-mounted (`DealGovernedTransitionPanel.tsx` in `BankerDealWorkspace.tsx`) — `docs/LOS_WORKFLOW_TRUTH_MATRIX.md`'s T7-T9 "preview-only" claim was stale; not corrected in this pass (documentation-only follow-up, not a code defect). Deep facts (risk rating, UW recommendation, approval-decision, commitment issuance, conditions-precedent, funds-disbursed) remain genuinely `tracked: false` — correctly non-blocking, not silently faked. The Dataverse governance plugin is written but **not compiled/registered/deployed** — every gate above is enforced 100% client-side; a direct Web API write bypasses all of it. This is the one item that makes D2's TRUE security floor operator-dependent, not code-dependent. | None — already correct; plugin registration is Phase 9/operator scope |
| D3 | Log Activity reported success without reconciling to CRM/timeline | **PARTIALLY FIXED (this pass)** | `src/crm/write/crmWriteAdapter.ts`'s `logActivity()` now cross-writes a matching `cr664_dealtimelineevents` row when `originatedDealId` is supplied, using the real event-type codes the deal Activity Timeline already recognizes (CallLogged/EmailLogged/NoteLogged/MeetingLogged). Reverse direction (deal-logged note appearing on the CRM timeline) remains a gap — out of scope for this pass, documented below. | **Fixed**: `src/crm/write/crmWriteAdapter.ts`, `.test.ts` (7 new tests) |
| D4 | Two inconsistent Log Activity experiences | **STILL REPRODUCIBLE (not fixed this pass)** | Two genuinely separate, differently-shaped modals/adapters/entities confirmed: `src/deals/LogActivityModal.tsx`+`logActivityActions.ts` (deal-scoped, note-only) vs. `src/crm/workspace/CrmWriteActions.tsx`+`crmWriteAdapter.ts` (CRM-scoped, activityType/summary/outcome/follow-up). Not unified this pass — a genuine UX-consolidation decision beyond a safe mechanical fix; flagged for a future workstream. | Not fixed — documented gap |
| D5 | Left navigation failed inside a deal cockpit | **VERIFIED FIXED** | `src/deals/BankerDealWorkspace.tsx:91-93` wires `onNavSelect`; regression test `BankerDealWorkspace.test.tsx` clicks a nav item and asserts real navigation. | None — already correct |
| D6 | Continue Workflow action did not navigate | **VERIFIED FIXED** | `src/banker/BankerLoanWorkflowWorkbench.tsx:68` — `openDeal` calls real `navigate(...)`; test asserts correct id. | None — already correct |
| D7 | Due-diligence counts disagreed across surfaces | **VERIFIED FIXED (core); one untested ETL seam)** | One canonical classifier (`documentStatusClassification.ts`) used by banker/manager/team; cross-view reconciliation test exists. Executive's `missingDocsCount` sources a separate governed Dataverse snapshot column by design (SPEC W2 isolation) — no test proves the ETL agrees with the live classifier; flagged, not fixed (would require touching an external ETL, out of this repo's scope). | None — code-side already correct |
| D8 | Task counts disagreed across workspaces | **FIXED (this pass)** | `src/manager/managerPipelineSnapshot.ts`'s `overdueTaskCount`/`closingNext30DayCount` used a raw `new Date()` on date-only fields (UTC-midnight bug), unlike Team/Banker (already on `parseCalendarDate` since Workstream H). Fixed to match; timezone-forced boundary regression tests added. | **Fixed**: `src/manager/managerPipelineSnapshot.ts`, `.test.ts` (2 new tests) |
| D9 | Manager data-quality buckets didn't reconcile to deal totals | **VERIFIED FIXED** | `deriveDataQualityDistribution` buckets are mutually exclusive + exhaustive by construction (`break` after first match); sum always equals `rows.length`. No property-style invariant test exists for arbitrary N — a coverage gap, not a defect; not added this pass given time constraints. | None — already correct |
| D10 | CRM company count disagreed with New Deal client picker | **VERIFIED FIXED for the original defect; residual scope gap is by-design** | Workstream D switched the picker to `loadClientLinkTargetOptions()`, the same bridged-company union the in-deal Link modal uses. CRM Hub's "Companies" tile intentionally counts ALL party types (not just loan-linkable ones) — an architecturally correct scope difference, not a bug, though it will never numerically match the picker. | None — already correct; documented scope note |
| D11 | Test stages/statuses/teams/records polluted operational views | **PARTIALLY FIXED** | Deal-level classification (`testDealClassification.ts`) is a deliberate, non-fragile bracketed-tag convention (`[SMOKE TEST - ...]`, etc.), consistently applied banker/manager/team/executive, with both contamination AND false-positive-avoidance tests. Reference-value seed scripts use a SEPARATE, broader `\b(test\|...)\b` convention — an inconsistency, low practical risk today (closed admin-curated label sets), not unified this pass. No test/team-naming convention exists for `cr664_Team` — not testable, no reproduction exists. | None — already correct where it matters; inconsistency documented |
| D12 | Duplicate/near-duplicate records not prevented or identified | **FIXED (this pass)** | `src/deals/newDealDuplicateDetection.ts`'s `norm()` only trimmed/lowercased/collapsed whitespace — legal-suffix/punctuation variants ("Acme LLC" vs "ACME, L.L.C." vs "acme llc") normalized to different keys, so genuine duplicates could go undetected. Strengthened; module's first dedicated test file added (16 tests) proving the fix and that unrelated names are never falsely flagged; merge remains hard-disabled regardless of config. | **Fixed**: `src/deals/newDealDuplicateDetection.ts`, new `.test.ts` |
| D13 | Boarded loan missing manager/status/rating/product mappings | **PARTIALLY FIXED (product + status confirmed correct by Workstream K + this pass; rating/manager/tie-out remain genuine schema gaps)** | Product: confirmed wired end-to-end (Workstream K). Status: confirmed honest (`'active'` business rule, not fabricated; `?? 'Not set'` fallback, never "Unknown"). Risk rating, portfolio manager, tie-out result: no source exists upstream — correctly left unpopulated per Workstream K's dependency report, not fabricated. | None new — already documented by Workstream K |
| D14 | Urgent dashboard messaging was misleading | **VERIFIED FIXED** | `GreetingHeader.tsx`'s urgent accent only renders `urgentCount > 0`; the count is a real derived sum (overdue tasks + overdue docs + past-target-close), tested. | None — already correct |
| D15 | Activity/Activity Log represented modified-record feeds, not interactions | **PARTIALLY FIXED** | `BankerActivityFeed.tsx` already relabeled + disclaimed ("Recent updates...", explicit "Not the per-deal Activity Timeline" copy, tested). `bankerPersonalActivity.ts`'s `staleActivityCount` and `PersonalPipeline.tsx`'s "Last touched" still silently use `modifiedon` under an "activity" label without a similar disclaimer; `deals/ActivityTimeline.tsx` still blends true interactions with system events in one undifferentiated list. Not fixed this pass (a labeling/IA change, not a safety defect). | Not fixed — documented gap |
| D16 | CRM linked deals were not navigable | **FIXED (this pass)** | `CrmHubWorkspace.tsx`'s "Linked deals" and `CrmRelationshipPanel.tsx`'s "Related deals (CRM)" both rendered dead `<li>`s. Both now navigate to `/deals/:id` via `useNavigate`, keyboard-accessible; regression tests prove correct-id navigation (not just "some navigation"). | **Fixed**: both files + both test files |
| D17 | Log Activity popover overlapped dashboard content | **SUPERSEDED** | `LogActivityModal.tsx` is a fixed, full-viewport, centered backdrop dialog (not an anchored popover) — structurally cannot overlap adjacent content. The only real `@radix-ui/react-popover` usage in the app is an unrelated CRM overflow menu, also not defective. If the original report described a since-removed component, current architecture already avoids this bug class. | None — no reproducible defect on current code |
| D18 | Executive KPIs were unavailable | **VERIFIED FIXED (core); non-core gaps honestly disclosed** | 10 real, wired KPI tiles on live derived data. Performance & Profitability panel is explicitly labeled "Not yet wired" (never a fabricated figure); `HonestOmissions` discloses what's intentionally not shown. This is disclosed limitation, not the original "unavailable" defect. | None — already correct |
| D19 | Mojibake in Admin headings | **FIXED (this pass)** | Three genuinely user/runtime-facing double-mis-encoded UTF-8 strings found and fixed: `AdminOperationsConsole.tsx`'s eyebrow heading, `GreetingHeader.tsx`'s read-only identity chip, `adminWorkspaceEntitlementQuery.ts`'s `REDACTED_OTHER` placeholder. Regression tests tightened/added to pin exact corrected strings (previous test only did a loose `.toMatch(/redacted/)` that silently tolerated the corruption). A large decorative ASCII-art comment in `GreetingHeader.tsx` remains corrupted but is developer-facing only (never rendered) — left as a documented, non-blocking cosmetic item to avoid unrelated large-diff churn. | **Fixed**: `AdminOperationsConsole.tsx`+test, `GreetingHeader.tsx`+new test, `adminWorkspaceEntitlementQuery.ts`+test |
| D20 | Modal interaction and Close behavior were unreliable | **PARTIALLY FIXED (this pass)** | Escape-key handling added to `LogActivityModal.tsx` and `DealProfileEditModal.tsx`, the two hand-rolled modals confirmed missing it (every other hand-rolled modal already had it). Close/X button already worked everywhere. Click-outside-to-dismiss remains unimplemented across EVERY hand-rolled modal in the app (a broader, pre-existing, consistent gap — not unique to these two) — not fixed this pass to avoid a large, under-tested sweep across 8+ components in one PR; documented as a follow-up. Focus-trap/return is also absent app-wide for hand-rolled modals (Radix-based `Dialog`/`CommandPalette` get it for free) — same follow-up note. | **Fixed (Escape only)**: `LogActivityModal.tsx`, `DealProfileEditModal.tsx` + 2 new tests |

## Summary counts

- **VERIFIED FIXED (no code change needed):** D5, D6, D7 (core), D9, D10 (core), D14, D17, D18 — 8
- **FIXED this pass (code + tests):** D3, D8, D12, D16, D19, D20 (Escape only) — 6
- **PARTIALLY FIXED, residual gap documented, not closed this pass:** D1 (data coverage), D2 (plugin
  registration — operator/Phase 9 scope), D11 (seed-script convention inconsistency), D13 (schema gaps
  — already Workstream K), D15 (IA/labeling) — 5
- **STILL REPRODUCIBLE, not fixed this pass:** D4 (UX consolidation decision, not a mechanical fix) — 1

No historical defect was found to be worse than previously documented; several (D1, D2's core wiring,
D7, D9, D10, D13's product field, D14, D18) were confirmed as already correctly resolved by prior
workstreams and needed no further action — "prove it, don't rebuild."
