# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Phase 122B — repair-script artifacts (runbook JSON + rollback zips).
# The script regenerates these on every dry-run; committing them
# would leak live Dataverse env URLs + timestamps.
.phase122/
