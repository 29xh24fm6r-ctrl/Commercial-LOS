# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Phase 122B — repair-script artifacts (runbook JSON + rollback zips).
# The script regenerates these on every dry-run; committing them
# would leak live Dataverse env URLs + timestamps.
.phase122/

# Phase 131A — local Claude Code agent workspace (memory, transcripts,
# project settings). Never committed; it holds session-local state.
.claude/

# CRM Intelligence — maker-provided / generated NAICS artifacts (not committed)
scripts/data/naics-2022.csv
scripts/data/*.seed.json
